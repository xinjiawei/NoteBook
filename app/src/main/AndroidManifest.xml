<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.journalnotebook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/main1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.journalnotebook.HandWriting.HandwritingActivity"></activity>

        <service
            android:name="com.example.journalnotebook.MyIntentService"
            android:exported="false" />

        <activity android:name="com.example.journalnotebook.EditActivity" />
        <activity android:name="com.example.journalnotebook.Alarm.EditAlarmActivity" />
        <activity android:name="com.example.journalnotebook.Alarm.AlarmActivity" />

        <receiver android:name="com.example.journalnotebook.Alarm.AlarmReceiver" />

        <activity android:name="com.example.journalnotebook.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

</manifest>